This directory is reserved for template-level or project-specific schemas.

- `base_schema.json` provides a starter definition that mirrors the shared `AgentState`.
- Example-only schemas live under `examples/schemas/` so the base template stays clean.


