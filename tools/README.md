This package now contains only the shared abstractions (e.g., `base_adapter.py`).

Example-specific adapters were moved to `examples/tools/` so that the template
remains clean for new integrations.



